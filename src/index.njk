---
layout: base.njk
---
<div class="hero">
  <h1>{{ site.name }}</h1>
  <p class="tagline">{{ site.tagline }}</p>
  <p class="intro">{{ site.intro }}</p>
</div>

<h2 class="section-heading">Selected work</h2>
{% set entries = scholarship | featured | concat(reports | featured, commentary | featured) %}
{% include "entry-list.njk" %}

<h2 class="section-heading">In progress</h2>
{% set entries = projects | featured %}
{% include "entry-list.njk" %}

<p class="currently">{{ site.currently }}</p>
